# Wetsuit Lab

An interactive, dependency-free SVG wetsuit designer with multiple seam maps, color and pattern fills, presets, undo, randomization, local saving, and PNG export.

## Run locally

```sh
npx serve .
```

## Deploy

```sh
npx vercel --prod
```
